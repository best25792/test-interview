meta {
  name: Get Transaction by ID
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/transactions/{{transactionId}}
  body: none
  auth: none
}

vars:pre-request {
  base_url: http://localhost:8080
  transactionId: 1
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response has transaction id", function() {
    expect(res.body.id).to.exist;
  });
}
